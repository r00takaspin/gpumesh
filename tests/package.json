{
  "name": "gpumesh-tests",
  "private": true,
  "scripts": {
    "test": "cucumber-js -p api --exit && cucumber-js -p ui --exit",
    "test:api": "cucumber-js -p api --exit",
    "test:ui": "cucumber-js -p ui --exit"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.9.0",
    "playwright": "^1.49.1"
  },
  "dependencies": {
    "ws": "^8.21.1"
  }
}
