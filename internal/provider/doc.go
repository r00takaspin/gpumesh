// Package provider implements the GPU Mesh provider agent: WebSocket client,
// Ollama model discovery, inference request proxying, and first-run wizard.
// Control plane (tokens, invites, members) lives on the coordinator /share UI.
package provider
